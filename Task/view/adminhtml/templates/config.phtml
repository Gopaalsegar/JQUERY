<div>
    <label for="search-key">Search Key:</label>
    <input type="text" id="search-key" placeholder="Enter key to search">
</div>

<table class="data table" id="custom-config-table">
    <thead>
    <tr>
        <th>Config</th>
        <th>Path</th>
        <th>Status</th>
    </tr>
    </thead>
    <tbody>
    <?php
    // Replace this PHP loop with your actual data
    foreach ($block->flattenConfigArray($block->getConfigArray()) as $data): ?>
        <tr>
            <td><?= $data['parent_key'] ?></td>
            <td><?= $data['key'] ?></td>
            <td><?= $data['value'] ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>